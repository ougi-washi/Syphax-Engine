# GitHub Pages Settings

Set Pages source to **GitHub Actions**.

If the live site shows `Jekyll v3.10.0`, no MkDocs navigation/search, or serves `.html` pages like `/path/sdf.html`, Pages is publishing the raw `docs/` source instead of the built `site/` artifact.

Verify deployed URL in workflow summary after each publish.
