Task: clean up `editor_sdf` save/load path handling and replace the local workaround with a simple `SE_MAX_PATH_LENGTH`-based fix

Scope
- Remove the `editor_sdf`-local path-resolution workaround that added custom path helpers and oversized local buffers.
- Keep `editor_sdf` save/load code simple and straightforward.
- Move the fix to the shared path-length contract by adjusting `SE_MAX_PATH_LENGTH` usage at the engine level.
- Preserve existing support for absolute paths and resource-relative paths.
- Keep autotests focused and readable; remove workaround-heavy long-path generation logic.

Implementation checklist
- Inspect the current `SE_MAX_PATH_LENGTH` definition and the places where it is used by editor path values and resource-path resolution.
- Confirm that the shared `se_paths_resolve_resource_path(...)` helper already handles absolute paths correctly.
- Revert `editor_sdf` save/load to use the simple shared resolution path instead of custom local path helpers.
- Replace any oversized local `editor_sdf` file-path buffers with `SE_MAX_PATH_LENGTH`.
- Increase `SE_MAX_PATH_LENGTH` to a more practical value so valid editor save/load paths do not fail from expansion/truncation.
- Remove the complex long-path autotest workaround from `editor_sdf --autotest`.
- Keep a small, high-signal save/load autotest that validates expected paths without path-construction tricks.
- Ensure no public API docs sync step is needed unless public headers changed.

Validation checklist
- Build `editor_sdf`.
- Run `editor_sdf --autotest` in headless terminal mode.
- Review resulting diff to ensure `editor_sdf` code is simpler than before.

Completion
- [x] Local workaround removed
- [x] `SE_MAX_PATH_LENGTH`-based fix implemented
- [x] Autotest simplified
- [x] Build passed
- [x] Runtime validation passed
