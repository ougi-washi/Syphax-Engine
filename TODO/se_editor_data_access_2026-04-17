- [x] Re-read the current `se_editor` public API and internal property storage code.
- [x] Re-read `examples/advanced/editor_sdf.c` property collection, property apply, UI, and autotest code.
- [x] Extend `include/se_editor.h` with property metadata types needed for richer editor data access.
- [x] Add `se_editor_property_role` to describe semantic roles such as position, rotation, scale, and color.
- [x] Add `se_editor_property_desc` to describe editable properties with ids, labels, role, and step sizes.
- [x] Extend `se_editor_property` to carry label, role, step, and large-step metadata.
- [x] Add new `se_editor` public helpers for property component count, property component name, and value component adjustment.
- [x] Keep `se_editor_add_property_value(...)` as a compatibility wrapper over the richer property-add path.
- [x] Implement the new property descriptor copy path in `src/se_editor.c`.
- [x] Implement the new component-count helper in `src/se_editor.c`.
- [x] Implement the new component-name helper in `src/se_editor.c`.
- [x] Implement the shared value-component adjustment helper in `src/se_editor.c`.
- [x] Ensure the new helper behavior covers scalar, `vec2`, `vec3`, and `vec4` values.
- [x] Ensure unsupported types such as raw matrices return a clear false path from value adjustment.
- [x] Refactor `editor_sdf` property collection to use stable dotted property ids.
- [x] Add short display labels for the collected SDF editor properties.
- [x] Assign semantic property roles and nudge step sizes during SDF property collection.
- [x] Replace SDF-local component count logic with the shared `se_editor` helpers.
- [x] Replace SDF-local component naming logic with the shared `se_editor` helpers.
- [x] Replace SDF-local value nudge logic with the shared `se_editor` helpers.
- [x] Add local transform decomposition helpers in `editor_sdf` for position, rotation, and scale editing.
- [x] Add local transform recomposition helpers in `editor_sdf` that rebuild the JSON matrix from TRS edits.
- [x] Expose node transforms as `transform.position`, `transform.rotation`, `transform.scale`, and `transform.matrix`.
- [x] Keep raw matrix editing available as an advanced escape hatch.
- [x] Convert SDF editor rotation display and input to degrees while keeping matrix math in radians internally.
- [x] Update SDF command-apply routing to use dotted property ids.
- [x] Make node transform position edits rewrite matrix translation through recomposition.
- [x] Make node transform rotation edits rewrite the transform matrix through recomposition.
- [x] Make node transform scale edits rewrite the transform matrix through recomposition.
- [x] Keep point-light move targets mapped to `position` rather than unrelated vector properties.
- [x] Add keyboard-only move mode state to `editor_sdf_app`.
- [x] Bind `M` to enter move mode from normal mode.
- [x] Bind `X`, `Y`, and `Z` to select the active movement axis while move mode is active.
- [x] Reuse `+` and `-` inputs for movement nudges while move mode is active.
- [x] Capture the original property value on move-mode entry so `Esc` can restore it.
- [x] Exit move mode cleanly on `Enter`.
- [x] Cancel move mode and restore the original value on `Esc`.
- [x] Disable selection navigation behavior while move mode owns the keyboard interaction.
- [x] Keep camera controls disabled during move mode so movement inputs do not conflict with camera inputs.
- [x] Refresh focus after successful transform or move updates so the selected object stays centered.
- [x] Update the on-screen help text for the new property labels, move mode, and axis feedback.
- [x] Extend the SDF autotest to cover the richer property surface.
- [x] Extend the SDF autotest to cover position edits through dotted property ids.
- [x] Extend the SDF autotest to cover move-mode commit behavior.
- [x] Extend the SDF autotest to cover move-mode cancel behavior.
- [x] Run `./scripts/docs/check_api_sync.sh` after public header changes.
- [x] Regenerate generated API docs if the sync check reports drift.
- [x] Build the affected target with `./build.sh editor_sdf`.
- [x] Run the headless autotest for `editor_sdf`.
- [x] Review diffs for `include/`, `src/`, `examples/advanced/`, `docs/api-reference/modules/`, and `TODO/`.
- [x] Mark this task checklist complete once implementation and validation are done.
