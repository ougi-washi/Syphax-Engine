Task: rename `editor_sdf` example state from `app` terminology to clearer editor-specific names

Scope
- Rename the local example state type `editor_sdf_app` to `sdf_editor`.
- Stop using `app` as the main state variable/parameter name in `editor_sdf.c`.
- Use `editor` for the example state pointer where practical.
- Rename local callback parameters of type `se_editor*` to `handle` to avoid name collisions and keep intent clear.
- Keep the change local to `examples/advanced/editor_sdf.c`.

Implementation checklist
- Update the typedef name from `editor_sdf_app` to `sdf_editor`.
- Rename forward declarations and function signatures to use `sdf_editor* editor`.
- Rename the `main` local state variable from `app` to `editor`.
- Rename `se_editor* editor` callback parameters to `se_editor* handle`.
- Preserve behavior exactly; this is a naming cleanup only.
- Build `editor_sdf` and run the existing autotest.

Validation checklist
- `./build.sh editor_sdf`
- `timeout 25s env SE_TERMINAL_RENDER=1 SE_TERMINAL_HIDE_WINDOW=1 ./bin/editor_sdf --autotest`

Completion
- [x] Type renamed
- [x] Local state params renamed
- [x] Callback handle params renamed
- [x] Build passed
- [x] Runtime validation passed
