Task: debug and fix repeated `editor_sdf :: save/load failed: invalid_argument` in `editor_sdf`

Scope
- Reproduce the save/load argument-validation failure in the current `editor_sdf` code path.
- Inspect the `editor_sdf` save/load helpers and the path-resolution utilities they depend on.
- Identify whether the failure comes from null/empty paths, path-length overflow, invalid resource-root joining, or state corruption.
- Patch the narrowest correct layer so save/load accepts the paths the editor actually stores and surfaces stable behavior.
- Preserve existing behavior for normal resource-relative scene paths.
- Preserve `/tmp/...` autotest save/load support.
- Avoid public API churn unless the root cause is in a shared public helper and the fix belongs there.

Implementation checklist
- Read `examples/advanced/editor_sdf.c` save/load helpers and command handlers line by line.
- Read `src/se_paths.c` resource-path resolution behavior line by line.
- Check current `SE_MAX_PATH_LENGTH` usage and any fixed-size buffers involved in editor save/load.
- Decide whether the bug should be fixed in `editor_sdf` only or in a shared helper.
- Patch the relevant code with explicit handling for already-absolute paths and persisted paths that may already include the resource root.
- Ensure save and load use the same normalization rules so a path saved into `app->json_path` remains reloadable.
- Add or extend `editor_sdf --autotest` coverage for the exact failure mode.
- Verify status/help text still reflects the actual save/load behavior.

Validation checklist
- Build `editor_sdf`.
- Run `editor_sdf --autotest` in headless terminal mode.
- Confirm the failing path shape now succeeds instead of returning `invalid_argument`.

Completion
- [x] Root cause identified
- [x] Fix implemented
- [x] Autotest coverage added or updated
- [x] Build passed
- [x] Runtime validation passed
